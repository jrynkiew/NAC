stages:
  - build-web
  - build-windows
  - build-linux

build-web:
  variables:
    BUILD_TARGET: "web"
    GIT_STRATEGY: clone
  tags:
    - web
  stage: build-web
  script:
    - ./install.sh
  
build-windows:
  variables:
    BUILD_TARGET: "windows"
    GIT_STRATEGY: clone
  tags:
    - windows
  stage: build-windows
  script:
    - ./install.sh

build-linux:
  variables:
    BUILD_TARGET: "linux"
    GIT_STRATEGY: clone
  tags:
    - linux
  stage: build-linux
  script:
    - ./install.sh